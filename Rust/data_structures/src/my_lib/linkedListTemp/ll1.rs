#[derive(Clone)]
enum address {
    address(Box<myList>), 
    none
}


#[derive(Clone)]
struct myNode {
    value:u32,
    next:Option<Box<myNode>>,
}

impl myList {
    fn append(&mut self,elem:u32) {
        match self.next {
sdf

        }
    }
}