Techstacks

- vite
- typescript + swc

dev

- "vite-tsconfig-paths" (tsconfig path resolution plugin)
- "eslint","vite-plugin-eslint"

```json
scripts {
  "lint:fix": "eslint . --fix",
  "lint": "eslint ."
}
```
