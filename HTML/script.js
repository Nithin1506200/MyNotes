/**
 * This is fibbinachi used to get output
 * Parameters:
 * - x is first number
 * - y is second number
 *
 * # examples
 *
 *
 * @param {int} x  is the first num
 * @param {float} y is the second num
 * @returns {float}
 */
function Fibbinachi(x, y) {
  return x + y;
}

let z = Fibbinachi();
